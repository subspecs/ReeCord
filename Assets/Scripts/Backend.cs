using UnityEngine;
using UnityEngine.UI;

public class Backend : MonoBehaviour
{
    public TMPro.TMP_Text TopAppTitle;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
